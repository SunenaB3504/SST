{
  "name": "nia-sst-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w nia-sst",
    "build": "npm run build -w nia-sst"
  }
}
